$(window).on("scroll", function () {
    AOS.init({
        mirror: false,
    });
});

$(window).on('load', function () {
    AOS.init();
  });

// $(window).on('load', function () {
//     AOS.refresh();
//   });
//   $(function () {
//     AOS.init();
//   });